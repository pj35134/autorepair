<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mechanic.autorepair">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Selectionofservice"></activity>
        <activity android:name=".Model_Info" />
        <activity android:name=".Models" />
        <activity android:name=".Main2Activity" />
        <activity
            android:name=".Navdrawer"
            android:label="@string/title_activity_navdrawer"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tab_using_fragments" />
        <activity android:name=".Editdetail" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Login_Activity" />
        <activity android:name=".MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>